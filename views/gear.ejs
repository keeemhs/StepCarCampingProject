<!DOCTYPE html>
<html lang="KR">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" />
        <!-- <style>
            .main1 {
                margin-left: 10px;
                margin-right: 10px;
                overflow-x: hidden; /* 가로 스크롤 숨김 */
                text-align: center;
                margin-top: 100px;
            }
            .no-dot {
                list-style-type: none;
                padding: 0;
                display: flex;
                flex-wrap: wrap; /* 아이템이 넘칠 경우 다음 줄로 이동하도록 수정 */
                justify-content: space-between; /* 아이템 간 간격 조정 */
            }
            .description {
                text-align: center;
            }
            .item {
                flex: 0 0 calc(25% - 20px); /* 4개 아이템을 한 줄에 표시하기 위해 너비를 조절합니다. */
                box-sizing: border-box;
                margin: 10px;
                max-width: calc(25% - 20px); /* 아이템의 최대 너비 설정 */
            }
            @media screen and (max-width: 1023px) {
                .display {
                    flex-wrap: wrap;
                }
                .item {
                    flex: 0 0 calc(50% - 20px); /* 화면이 작을 때는 2개씩 보이도록 설정합니다. */
                }
            }

            /* 더 작은 화면 크기에서는 한 개씩 아이템을 표시합니다. */
            @media screen and (max-width: 767px) {
                .item {
                    flex: 0 0 calc(100% - 20px); /* 화면이 아주 작을 때는 한 개씩 보이도록 설정합니다. */
                }
            }
        </style> -->

        <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous" />
        <style>
            .grid-container {
                display: grid;
                /* 3등분 */
                gap: 10px 20px;
                grid-template-columns: 1fr 1fr 1fr;
                align-items: center;
            }
            a {
                text-decoration-line: none;
                color: black;
            }
            #sorting {
                text-align: right;
                /* justify-content: right; */
            }
            .card {
                background-color: ghostwhite;
            }
        </style>

        <title>Document</title>
    </head>
    <body>
        <!-- <%-include('header.ejs')%>

        <section class="main1">
            <h2>제품 대여</h2>

            <div>
                <button onclick="location.href='/gear/gearreviewEdit'">새로작성</button>
            </div>
        </section>

        <%-include('footer.ejs')%> -->

        <%-include('header.ejs')%>

        <hr />

        <main>
            <div>후기</div>
            <hr />
            <div class="grid-container">
                <% for (let i =0; i< data.length ;i++){ %>
                <a href="/gear/gearreview?gearId=<%= data[i].gearid %>">
                    <div class="card" style="width: 18rem">
                        <!-- galleries.imgurl -->
                        <img src="https://hwr-bucket.s3.ap-northeast-2.amazonaws.com/<%= data[i].thunmnail %>" class="card-img-top" alt="..." />
                        <div class="card-body">
                            <!-- galleries.title -->
                            <h5 class="card-title"><%= data[i].gearTitle %></h5>
                        </div>
                    </div>
                </a>
                <% } %>
            </div>
            <hr />
            <div>
                <button onclick="location.href='/gear/gearreviewEdit'">새로작성</button>
            </div>
        </main>
        <hr />
        <%-include('footer.ejs')%>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
        <script>
            async function fileupload(event) {
                event.preventDefault();
                const form = document.forms['axiosForm'];
                const formDatas = new FormData();
                const files = form.dynamic_files.files;
                console.log(files);
                for (let i = 0; i < files.length; i++) {
                    formDatas.append(`array_files`, files[i]);
                }
                const result = await axios({
                    url: '/gear/multiAxios',
                    method: 'POST',
                    data: formDatas,
                    Headers: {
                        'Content-Type': 'multipart/form-data',
                    },
                });
                console.log(result.data);
            }
        </script>
    </body>
</html>
